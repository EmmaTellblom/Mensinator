CREATE TABLE note_date(
id INTEGER PRIMARY KEY AUTOINCREMENT,
date TEXT NOT NULL
);


-- SELECT QUERIES --
--------------------

getAllDatesByNote:
SELECT note_date.*
FROM note_date
INNER JOIN note_date_cross_ref nd_cr ON note_date.id = nd_cr.note_date_id
INNER JOIN note ON nd_cr.note_id = note.id
WHERE note.name = ?;


-- INSERT QUERIES --
--------------------

insertNoteDate:
INSERT OR REPLACE INTO note_date(date) VALUES (?);


-- UPDATE QUERIES --
--------------------


-- DELETE QUERIES --
--------------------
